#include "main.h"
/**
 * mul - Multiplies two integers.
 * @a: One of the numbers to be checked.
 * @b: One of the numbers to be checked.
 * Return: Always 0.
 */

int mul(int a, int b)
{
	return (a * b);
}
